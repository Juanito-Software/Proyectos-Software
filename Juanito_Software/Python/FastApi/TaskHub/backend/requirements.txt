fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.36
pydantic[email]==2.9.0
pydantic-settings==2.5.0
# PyJWT en lugar de python-jose.
#
# python-jose estuvo cuatro anos sin publicar version (3.3.0 en 2021, 3.4.0 en
# 2025) y en ese hueco arrastro una confusion de algoritmos sin corregir. La
# documentacion oficial de FastAPI dejo de recomendarlo por eso.
#
# Ademas unifica: LeaderBoard_Unity ya usa PyJWT, asi que ahora hay una sola
# libreria de JWT en el repositorio en lugar de dos que hacen lo mismo.
#
# No hace falta el extra [cryptography]: con HS256 basta la implementacion
# propia de PyJWT. Solo seria necesario para algoritmos RSA o ECDSA.
PyJWT==2.13.0
bcrypt==5.0.0
# python-multipart lo usa FastAPI para leer formularios. Aqui interviene en el
# login, via OAuth2PasswordRequestForm, que es una ruta publica y sin
# autenticar: cualquiera puede enviarle un formulario deformado. Por eso las
# denegaciones de servicio al parsear (cabeceras de parte sin limite, frontera
# malformada, parseo cuadratico) son alcanzables sin credenciales.
python-multipart==0.0.32
python-dotenv==1.2.2
