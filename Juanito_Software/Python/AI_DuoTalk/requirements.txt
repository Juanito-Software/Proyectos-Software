# Dependencias principales para el sistema de IAs de voz

# PyTorch con soporte CUDA (opcional, pero recomendado)
# Para Windows con CUDA 11.8:
# torch --index-url https://download.pytorch.org/whl/cu118
# Para CPU solamente:
torch>=2.0.0

# Whisper para Speech-to-Text
openai-whisper>=20231117

# GPT4All para LLM local
gpt4all>=2.5.0

# Silero TTS (se carga vía torch.hub, pero necesitamos estas dependencias)
sounddevice>=0.4.6
numpy>=1.24.0
soundfile>=0.12.1
omegaconf>=2.3.0
pyyaml>=5.1.0

# Utilidades de audio
pydub>=0.25.1
librosa>=0.10.0  # Para cargar audio sin depender de FFmpeg

# API web (opcional, para futuras extensiones)
fastapi>=0.104.0
uvicorn>=0.24.0

# Utilidades adicionales
scipy>=1.11.0

# Detección de teclado para atajos (Ctrl+B para parar debate)
keyboard>=0.13.5

