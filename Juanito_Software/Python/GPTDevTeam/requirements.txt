# =========================
# GPTDevTeam runtime dependencies
# =========================
#
# GPTDevTeam_v2.0.py / GPTDevTeam_v1.3.py no cargan ningun modelo en proceso:
# todo el trabajo de LLM se delega a un servidor Ollama local via HTTP
# (http://localhost:11434/api/generate). La unica dependencia externa real
# es "requests"; todo lo demas que usan los scripts (ast, json, os, re,
# subprocess, tempfile, threading, etc.) es libreria estandar de Python.
#
# Requisito aparte (no es un paquete pip): tener Ollama instalado y corriendo
# con los modelos qwen3:8b y qwen2.5-coder:7b descargados (`ollama pull ...`).

requests
