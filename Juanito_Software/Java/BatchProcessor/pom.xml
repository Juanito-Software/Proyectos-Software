<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.3.4</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.example</groupId>
	<artifactId>BatchProcessor</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>BatchProcessor</name>
	<description>Demo project for Equipos Spring Boot</description>
	<properties>
		<java.version>17</java.version>
		<map.struct.version>1.5.5.Final</map.struct.version>
	</properties>

	<repositories>
		<repository>
			<id>central</id>
			<url>https://repo.maven.apache.org/maven2</url>
		</repository>
	</repositories>


	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.batch</groupId>
			<artifactId>spring-batch-core</artifactId>
			<version>5.2.0</version>  <!-- la versión más reciente disponible -->
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-batch</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>

		<!--
			Coordenadas nuevas del conector de MySQL.

			El artefacto antiguo, mysql:mysql-connector-java, esta descatalogado
			desde 2022: Oracle movio el conector a com.mysql:mysql-connector-j y
			dejo de publicar el anterior, que se quedo en la 8.0.33 con la
			vulnerabilidad sin parchear. No es una subida de version, es un
			cambio de artefacto.

			Sin <version>: la fija el spring-boot-starter-parent, que la mantiene
			al dia y coherente con el resto del stack.
		-->
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!--
			H2 en memoria, solo para tests.

			El unico test que habia, contextLoads, no podia ejecutarse en
			ninguna parte salvo con un MySQL levantado en localhost:3306: eso
			lo dejaba fuera del CI y lo convertia en un test que solo pasaba
			en la maquina de quien lo escribio. Con H2 el contexto arranca sin
			nada instalado, que es lo que permite que el CI lo ejecute.

			Alcance test: no entra en el artefacto final ni sustituye a MySQL,
			que sigue siendo la base de datos de la aplicacion.
		-->
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.batch</groupId>
			<artifactId>spring-batch-test</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>2.1.0</version>
		</dependency>

		<dependency>
			<groupId>com.fasterxml.jackson.dataformat</groupId>
			<artifactId>jackson-dataformat-csv</artifactId>
			<version>2.15.2</version> <!-- Usa la versión compatible con tu proyecto -->
		</dependency>

		<!--
			Aqui estaban springfox-swagger2 y springfox-swagger-ui 3.0.0. Se han
			retirado y conviene saber por que, para que no vuelvan.

			Springfox y springdoc son dos implementaciones alternativas de lo
			mismo: exponer la documentacion OpenAPI de la API. El proyecto usa
			springdoc -SwaggerConfig construye un GroupedOpenApi y un OpenAPI,
			que son clases suyas- y no habia una sola importacion de io.springfox
			en todo el codigo.

			Ademas no podian funcionar aunque se quisiera: springfox 3.0.0 es de
			2020 y esta compilado contra javax.servlet. Spring Boot 3 migro a
			jakarta.servlet, asi que sus clases revientan con NoClassDefFoundError
			en cuanto se tocan. El proyecto arrancaba porque nadie las tocaba.

			Lo que dejaban a cambio: dos arboles de dependencias transitivas en
			el classpath -y en el jar final- que nadie audita, porque nadie sabe
			que estan ahi.
		-->

	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.13.0</version>
				<configuration>
					<source>${java.version}</source>
					<target>${java.version}</target>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
