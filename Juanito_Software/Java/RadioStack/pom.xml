<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.radiostack</groupId>
    <artifactId>radiostack</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>radiostack</name>
    <description>RadioStack - plataforma de gestión de radio online</description>

    <modules>
        <module>radiostack-core</module>
        <module>radiostack-persistence</module>
        <module>radiostack-api</module>
        <module>radiostack-admin</module>
        <!-- Módulo futuro para streaming -->
        <!-- <module>radiostack-stream</module> -->
    </modules>

    <properties>
        <java.version>17</java.version>
        <spring-boot.version>3.2.5</spring-boot.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <javafx.version>21.0.2</javafx.version>
        <jjwt.version>0.12.6</jjwt.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

	    <dependency>
    		<groupId>org.springdoc</groupId>
    		<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
    		<version>2.5.0</version>
	    </dependency>

            <!--
                JJWT: firma y verificacion de los tokens de acceso.
                Se declara aqui la version (BOM manual) para que los tres
                artefactos del modulo api no puedan desincronizarse entre si.
            -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.11.0</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <!--
                            Conserva en el .class el nombre de los parametros de
                            cada metodo. Sin esto, javac los borra y quedan como
                            arg0, arg1..., y Spring MVC no puede resolver un
                            @PathVariable o un @RequestParam que no diga su
                            nombre: al llegar la peticion lanza

                              Name for argument of type [java.lang.Long] not
                              specified, and parameter name information not
                              available via reflection

                            que el cliente ve como un 500. Afectaba a 11 metodos
                            de 5 controladores: todo /api/v1/emisiones/{id}/...,
                            los comentarios, los programas y el activar y
                            desactivar de locutores.

                            No estaba porque este proyecto NO hereda de
                            spring-boot-starter-parent —solo importa su BOM de
                            versiones—, y esta bandera es una de las cosas que
                            ese padre configura por defecto. Al montar el pom a
                            mano se quedo fuera, y como ningun test levantaba la
                            capa web, nada lo delataba.

                            Lo encontro SecurityConfigTest, el primer test que
                            atraviesa el DispatcherServlet de verdad.
                        -->
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
